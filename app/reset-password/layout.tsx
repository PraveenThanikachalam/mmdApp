import { ReactNode } from "react";

export default function ForgotPassoword_Layout({ children }: { children: ReactNode }){
    return (
        <div className="flex h-screen justify-center items-center" >
            {children}
        </div>
    )
}